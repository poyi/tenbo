# {{layer_name}}

## Purpose
<!-- One paragraph. Why this layer exists. The user-facing job it does. -->

## Responsibilities
- <!-- Verb phrases. ≤ 7 items. -->

## Optimizing for
<!--
1–3 dimensions. NOT the same as Responsibilities:
  Responsibilities = ownership ("this layer owns X").
  Optimizing-for = trade-off direction ("when balancing X vs Y, this
  layer favors Y").
Use this when two reasonable design choices conflict — name which side
this layer leans toward and why. Example: "Read latency over write
ergonomics — reads happen on every page load; writes are rare."
-->
- **<dimension>**: <one sentence on the trade-off direction>

## Anti-responsibilities
- <!-- "Does NOT do X — that lives in <Layer Y>." ≤ 5 items. -->

## Boundaries
- **Upstream (depends on):** <!-- layers and what's consumed -->
- **Downstream (depended on by):** <!-- layers and what's provided -->
- **External:** <!-- libraries/services and roles -->

## Invariants
- <!-- Things that must always be true. ≤ 5 items. -->

## Boundary decisions
<!-- Newest first. Append-only by agent. Split into decisions.md when ~10+ entries. -->
- {{today}}: created — initial intent draft.
