# {{repo_name}} — Overview

<2–3 sentences: the elevator pitch. What does this app/repo do, who is it for, what problem does it solve? Plain English, no jargon.>

## Product goals

<!--
3–6 goals max. IDs g1/g2/g3/... (lowercase g + integer, no padding).
Each goal is ONE sentence describing what success looks like along that
dimension. These are the north stars an agent checks new ideas against
at intake, recommend, and audit time. Skip vague platitudes ("be great") —
goals should be specific enough to disagree with.
-->
- **g1**: <one sentence — what does success look like on this dimension?>
- **g2**: <one sentence>
- **g3**: <one sentence>

## Non-goals

<!--
List things explicitly OUTSIDE scope. Helps the agent push back on
misaligned ideas without re-deriving the boundary every time. One line
each. Example: "Not a general-purpose project tracker — AI-assisted
coding workflows only."
-->
- <one non-goal>

## Scopes

<One subsection per scope. Each is 2–3 sentences pointing at the scope's role and linking to its layers.>

### {{scope.name}} (`{{scope.path}}`)

{{scope.description}}

Layers: <comma-separated list of layer names linking to their narrative .md files>

## Cross-cutting concerns

<Optional. Concerns that span scopes — e.g., shared design tokens, telemetry. One line each.>

## How this overview is maintained

This file is generated and updated by the `tenbo` skill from the data in `workspace.yaml`, each scope's `architecture.yaml`, and the layer narratives. Edits to layer descriptions should happen in the source files; re-render here.
