# {{repo_name}} — Overview

<2–3 sentences: the elevator pitch. What does this app/repo do, who is it for, what problem does it solve? Plain English, no jargon.>

## Scopes

<One subsection per scope. Each is 2–3 sentences pointing at the scope's role and linking to its layers.>

### {{scope.name}} (`{{scope.path}}`)

{{scope.description}}

Layers: <comma-separated list of layer names linking to their narrative .md files>

## Cross-cutting concerns

<Optional. Concerns that span scopes — e.g., shared design tokens, telemetry. One line each.>

## How this overview is maintained

This file is generated and updated by the `tenbo` Claude Code skill from the data in `workspace.yaml`, each scope's `architecture.yaml`, and the layer narratives. Edits to layer descriptions should happen in the source files; re-render here.
